# Copy to .env (git-ignored) and set ONE of these. See the README for how to get
# a token. Do NOT commit real values.

# A long-term OAuth token from `claude setup-token` (uses your existing Claude plan):
CLAUDE_CODE_OAUTH_TOKEN=

# ...or an Anthropic API key (API billing):
# ANTHROPIC_API_KEY=
